SET(REL Client)

add_subdirectory(Core)
add_subdirectory(exe)

SET(INCLUDE_DIRS ${INCLUDE_DIRS} PARENT_SCOPE)
SET(SOURCE_EXE ${SOURCE_EXE} PARENT_SCOPE)
SET(EXE_NAMES ${EXE_NAMES} PARENT_SCOPE)

#SET(SOURCE_CPP ${SOURCE_CPP} PARENT_SCOPE)
